<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.compnayservice">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CompnayService">
        <activity android:name=".OrderServices"></activity>
        <activity android:name=".LoginCompany" />
        <activity android:name=".CustomerList" />
        <activity android:name=".RegisterUser" />
        <activity android:name=".Settings" />
        <activity android:name=".MainScreen" />
        <activity android:name=".RegisterScreen" />
        <activity android:name=".LoginScreen" />
        <activity android:name=".WelcomeScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>